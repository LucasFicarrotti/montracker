import 'package:flutter/material.dart';

class AddNewCashFlowPage extends StatelessWidget {
  const AddNewCashFlowPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('New cash flow'),
    );
  }
}